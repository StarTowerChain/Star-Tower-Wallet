enum CoinManagerEnum { hd, network }

enum ChainActionTypeEnum { import, create }

enum MnemonicOriginEnum {
  hd,
  createAccount,
}

enum CoinEnum { btc, eth, trx, bsc, demon }

//
enum WalletManagerEnum { dialog, page }

enum IdentityCreateType { creteAll, addNew }

enum TransferFeeEnum { slow, normal, fast, custom }

enum Language { zh_cn, zh_tw, en, ko, jp, laos }
